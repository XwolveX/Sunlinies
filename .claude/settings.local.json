{
  "permissions": {
    "allow": [
      "Bash(Get-ChildItem -Recurse -Force)",
      "Bash(Select-Object -ExpandProperty FullName)",
      "PowerShell(head -100)",
      "Bash(mvn compile *)"
    ]
  }
}
